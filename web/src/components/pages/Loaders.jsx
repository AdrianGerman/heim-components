import Section from "../Section"
import { components } from "../../data/components"

const Loaders = () => {
  return <Section title="Loaders" data={components.loaders}></Section>
}

export default Loaders
