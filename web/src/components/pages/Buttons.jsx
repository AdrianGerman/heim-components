import Section from "../Section"
import { components } from "../../data/components"

const Buttons = () => {
  return <Section title="Buttons" data={components.buttons}></Section>
}

export default Buttons
