import Section from "../Section"
import { components } from "../../data/components"

const Images = () => {
  return <Section title="Images" data={components.images}></Section>
}

export default Images
